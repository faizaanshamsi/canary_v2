<h1>Your photos for <%= @item.title %></h1>

<%= link_to "Add additional photos", new_item_item_photo_path(@item), class: 'button'%><br>
<h1>Item Photos</h1>

<% @item_photos.each do |item_photo| %>
  <%= filepicker_image_tag item_photo.photo, w: 200, h: 200, fit: 'clip' %><br>
<% end %>
